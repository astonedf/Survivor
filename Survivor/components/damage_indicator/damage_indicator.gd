extends Node2D

#var _number_pop_scene = preload("res://ui/number_pop/number_pop.tscn")
#var _parent: Character = null
#
#func _ready() -> void:
	#_parent = get_parent()
	#_parent.damage_taken.connect(_on_damage_taken)
	#
	#
#func _on_damage_taken(source: AreaOfEffect, amount: int) -> void:
	#var number_pop: NumberPop = _number_pop_scene.instantiate()
	#add_child(number_pop)
	#number_pop.pop(str(amount), Color(1.0, 1.0, 1.0))
	
